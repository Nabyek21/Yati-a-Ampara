---
// Componente: Botón de AmparIA en la esquina inferior derecha
// AmparIA es el asistente educativo de la plataforma Yatiña
---

<button 
  id="ia-header-btn" 
  class="ia-header-button" 
  title="Abrir AmparIA"
  aria-label="AmparIA - Asistente Educativo"
>
  <i class="fas fa-robot"></i>
  <span class="ia-button-tooltip">AmparIA</span>
</button>

<!-- Contenedor del Chat (oculto por defecto) -->
<div id="ia-header-chat-container" class="ia-chat-hidden">
  <div id="ia-chatbot-backdrop" class="ia-chatbot-backdrop"></div>
  
  <div id="ia-chatbot-panel" class="ia-chatbot-panel">
    <div class="ia-chatbot-header">
      <div class="ia-chatbot-title">
        <i class="fas fa-robot"></i>
        <div>
          <h3>AmparIA</h3>
          <p>Tu asistente educativo</p>
        </div>
      </div>
      <button id="ia-header-close" class="ia-chatbot-close-btn" title="Cerrar">
        <i class="fas fa-times"></i>
      </button>
    </div>

    <div id="ia-header-messages" class="ia-chatbot-messages">
      <div class="ia-chatbot-welcome">
        <div class="ia-welcome-icon">🤖</div>
        <h4>¡Hola! Soy AmparIA</h4>
        <p>Tu asistente educativo de Yatiña. Puedo ayudarte con:</p>
        <ul>
          <li>📚 Explicar conceptos</li>
          <li>❓ Responder preguntas</li>
          <li>📋 Generar guías</li>
          <li>📊 Analizar tu progreso</li>
        </ul>
      </div>
    </div>

    <div class="ia-chatbot-input-area">
      <form id="ia-header-form" class="ia-chatbot-input-form">
        <input
          type="text"
          id="ia-header-input"
          class="ia-chatbot-input"
          placeholder="Escribe tu pregunta..."
          autocomplete="off"
        />
        <button type="submit" class="ia-chatbot-send-btn" title="Enviar">
          <i class="fas fa-paper-plane"></i>
        </button>
      </form>
    </div>
  </div>
</div>

<style is:global>
/* Botón en Header */
.ia-header-button {
  position: fixed;
  bottom: 20px;
  right: 20px;
  z-index: 9997;
  width: 50px;
  height: 50px;
  border-radius: 50%;
  background: linear-gradient(135deg, #7c3aed, #6d28d9);
  color: white;
  border: none;
  cursor: pointer;
  font-size: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 12px rgba(124, 58, 237, 0.4);
  transition: all 0.3s ease;
  padding: 0;
}

.ia-header-button:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 20px rgba(124, 58, 237, 0.6);
}

.ia-header-button:active {
  transform: scale(0.95);
}

.ia-button-tooltip {
  position: absolute;
  bottom: -35px;
  right: 0;
  background: rgba(0, 0, 0, 0.8);
  color: white;
  padding: 6px 12px;
  border-radius: 4px;
  font-size: 12px;
  font-weight: 500;
  white-space: nowrap;
  opacity: 0;
  pointer-events: none;
  transition: opacity 0.3s ease;
}

.ia-header-button:hover .ia-button-tooltip {
  opacity: 1;
}

/* Chat Container */
.ia-chat-hidden {
  display: none;
}

.ia-chatbot-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.3);
  animation: fadeIn 0.3s ease;
  z-index: 9998;
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.ia-chatbot-panel {
  position: fixed;
  bottom: 80px;
  right: 20px;
  width: 100%;
  max-width: 400px;
  height: 600px;
  background: white;
  border-radius: 12px;
  display: flex;
  flex-direction: column;
  box-shadow: 0 -5px 40px rgba(0, 0, 0, 0.16);
  animation: slideUpChat 0.3s ease;
  z-index: 9999;
}

@keyframes slideUpChat {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.ia-chatbot-header {
  padding: 16px;
  background: linear-gradient(135deg, #7c3aed, #6d28d9);
  color: white;
  border-radius: 12px 12px 0 0;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 12px;
}

.ia-chatbot-title {
  display: flex;
  gap: 12px;
  align-items: center;
  flex: 1;
}

.ia-chatbot-title i {
  font-size: 24px;
}

.ia-chatbot-title h3 {
  margin: 0;
  font-size: 16px;
  font-weight: 700;
}

.ia-chatbot-title p {
  margin: 0;
  font-size: 12px;
  opacity: 0.9;
}

.ia-chatbot-close-btn {
  background: rgba(255, 255, 255, 0.2);
  border: none;
  color: white;
  cursor: pointer;
  font-size: 18px;
  width: 36px;
  height: 36px;
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all 0.2s ease;
}

.ia-chatbot-close-btn:hover {
  background: rgba(255, 255, 255, 0.3);
}

.ia-chatbot-messages {
  flex: 1;
  overflow-y: auto;
  padding: 16px;
  display: flex;
  flex-direction: column;
  gap: 12px;
  background: #f9fafb;
}

.ia-chatbot-welcome {
  text-align: center;
  padding: 20px;
  background: white;
  border-radius: 8px;
  border: 2px dashed #e5e7eb;
}

.ia-welcome-icon {
  font-size: 40px;
  margin-bottom: 12px;
}

.ia-chatbot-welcome h4 {
  margin: 0 0 8px 0;
  font-size: 16px;
  color: #1f2937;
}

.ia-chatbot-welcome p {
  margin: 0 0 12px 0;
  font-size: 12px;
  color: #6b7280;
}

.ia-chatbot-welcome ul {
  margin: 0;
  padding-left: 20px;
  text-align: left;
  font-size: 12px;
  color: #374151;
}

.ia-chatbot-welcome li {
  margin: 4px 0;
}

.ia-message {
  display: flex;
  gap: 8px;
  animation: slideInMessage 0.3s ease;
}

@keyframes slideInMessage {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.ia-message.user {
  justify-content: flex-end;
}

.ia-message-content {
  max-width: 85%;
  padding: 12px 14px;
  border-radius: 8px;
  word-wrap: break-word;
  line-height: 1.4;
  font-size: 13px;
}

.ia-message-bot .ia-message-content {
  background: white;
  color: #1f2937;
  border: 1px solid #e5e7eb;
}

.ia-message-user .ia-message-content {
  background: linear-gradient(135deg, #7c3aed, #6d28d9);
  color: white;
  border-radius: 18px;
}

.ia-message-loading {
  display: flex;
  gap: 4px;
  padding: 12px 14px;
  background: white;
  border: 1px solid #e5e7eb;
  border-radius: 8px;
}

.ia-message-loading span {
  width: 6px;
  height: 6px;
  background: #7c3aed;
  border-radius: 50%;
  animation: pulse 1.4s infinite;
}

.ia-message-loading span:nth-child(2) {
  animation-delay: 0.2s;
}

.ia-message-loading span:nth-child(3) {
  animation-delay: 0.4s;
}

@keyframes pulse {
  0%, 100% {
    opacity: 0.4;
  }
  50% {
    opacity: 1;
  }
}

.ia-chatbot-input-area {
  padding: 12px;
  border-top: 1px solid #e5e7eb;
  background: white;
}

.ia-chatbot-input-form {
  display: flex;
  gap: 8px;
}

.ia-chatbot-input {
  flex: 1;
  padding: 10px 12px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 13px;
  font-family: inherit;
  transition: all 0.2s ease;
}

.ia-chatbot-input:focus {
  outline: none;
  border-color: #7c3aed;
  box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.1);
}

.ia-chatbot-send-btn {
  padding: 10px 12px;
  background: linear-gradient(135deg, #7c3aed, #6d28d9);
  color: white;
  border: none;
  border-radius: 6px;
  cursor: pointer;
  font-size: 14px;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  justify-content: center;
}

.ia-chatbot-send-btn:hover {
  transform: translateY(-2px);
  box-shadow: 0 4px 12px rgba(124, 58, 237, 0.4);
}

.ia-chatbot-send-btn:active {
  transform: translateY(0);
}

/* Scroll suave */
.ia-chatbot-messages::-webkit-scrollbar {
  width: 6px;
}

.ia-chatbot-messages::-webkit-scrollbar-track {
  background: transparent;
}

.ia-chatbot-messages::-webkit-scrollbar-thumb {
  background: #cbd5e1;
  border-radius: 3px;
}

.ia-chatbot-messages::-webkit-scrollbar-thumb:hover {
  background: #94a3b8;
}

/* Responsive */
@media (max-width: 768px) {
  .ia-header-button {
    top: 10px;
    right: 10px;
    width: 45px;
    height: 45px;
    font-size: 18px;
  }

  .ia-chatbot-panel {
    top: 70px;
    right: 10px;
    left: 10px;
    max-width: none;
  }
}
</style>

<script>
import { chatConIA } from '../services/iaService.js';

document.addEventListener('DOMContentLoaded', () => {
  const headerBtn = document.getElementById('ia-header-btn');
  const chatContainer = document.getElementById('ia-header-chat-container');
  const backdrop = document.getElementById('ia-chatbot-backdrop');
  const closeBtn = document.getElementById('ia-header-close');
  const messagesDiv = document.getElementById('ia-header-messages');
  const form = document.getElementById('ia-header-form');
  const input = document.getElementById('ia-header-input');

  let sessionId = `session_${Date.now()}`;
  let isOpen = false;

  function openChat() {
    chatContainer.classList.remove('ia-chat-hidden');
    isOpen = true;
    input.focus();
  }

  function closeChat() {
    chatContainer.classList.add('ia-chat-hidden');
    isOpen = false;
  }

  headerBtn.addEventListener('click', () => {
    if (isOpen) {
      closeChat();
    } else {
      openChat();
    }
  });

  closeBtn.addEventListener('click', closeChat);
  backdrop.addEventListener('click', closeChat);

  function getContextoModulo() {
    const pathParts = window.location.pathname.split('/').filter(p => p);
    
    // Obtener id_curso (último o penúltimo segmento dependiendo de la URL)
    let id_curso = null;
    let id_modulo = null;
    
    // Si estamos en una página de curso: /docente/curso/1
    if (pathParts.includes('curso') || pathParts.includes('student')) {
      const indice = pathParts.findIndex(p => p === 'curso' || p === 'student');
      if (indice >= 0 && indice < pathParts.length - 1) {
        id_curso = parseInt(pathParts[indice + 1]);
      }
    }
    
    // Si estamos en una página de módulo: /student/curso/1/modulo/2
    if (pathParts.includes('modulo')) {
      const indice = pathParts.findIndex(p => p === 'modulo');
      if (indice >= 0 && indice < pathParts.length - 1) {
        id_modulo = parseInt(pathParts[indice + 1]);
      }
    }
    
    return { 
      id_curso: id_curso || 1,
      id_modulo: id_modulo || null
    };
  }

  function mostrarMensaje(texto, esUsuario = false) {
    const mensaje = document.createElement('div');
    mensaje.className = `ia-message ${esUsuario ? 'ia-message-user' : 'ia-message-bot'}`;
    
    const contenido = document.createElement('div');
    contenido.className = 'ia-message-content';
    contenido.textContent = texto;
    
    mensaje.appendChild(contenido);
    messagesDiv.appendChild(mensaje);
    
    setTimeout(() => {
      messagesDiv.scrollTop = messagesDiv.scrollHeight;
    }, 100);
  }

  function mostrarCargando() {
    const mensaje = document.createElement('div');
    mensaje.className = 'ia-message ia-message-bot';
    mensaje.id = 'ia-loading-message';
    
    const contenido = document.createElement('div');
    contenido.className = 'ia-message-loading';
    contenido.innerHTML = '<span></span><span></span><span></span>';
    
    mensaje.appendChild(contenido);
    messagesDiv.appendChild(mensaje);
    
    messagesDiv.scrollTop = messagesDiv.scrollHeight;
  }

  function eliminarCargando() {
    const loadingMsg = document.getElementById('ia-loading-message');
    if (loadingMsg) loadingMsg.remove();
  }

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    
    const texto = input.value.trim();
    if (!texto) return;
    
    input.value = '';
    mostrarMensaje(texto, true);
    mostrarCargando();
    
    try {
      const contexto = getContextoModulo();
      // Pasar id_modulo si está disponible para búsqueda específica del módulo
      const respuesta = await chatConIA(texto, sessionId, contexto, contexto.id_modulo);
      
      // Actualizar sessionId para la próxima solicitud
      if (respuesta.sessionId) {
        sessionId = respuesta.sessionId;
      }
      
      eliminarCargando();
      
      if (respuesta.error) {
        mostrarMensaje('Lo siento, ocurrió un error. Intenta nuevamente.');
      } else {
        mostrarMensaje(respuesta.respuesta || respuesta.message || 'No hay respuesta');
      }
    } catch (error) {
      eliminarCargando();
      console.error('Error:', error);
      mostrarMensaje('Error: No pude procesar tu pregunta. Intenta otra cosa.');
    }
  });
});
</script>
